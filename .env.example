# Адрес API сервера (без слэша в конце). По умолчанию — прод-адрес из кода.
VITE_API_URL=https://135.106.211.85.nip.io
# VK ID владельца приложения: только ему открывается админка в UI.
# Серверная проверка прав — отдельно через ADMIN_VK_IDS в server/.env.
VITE_ADMIN_VK_ID=000000000
