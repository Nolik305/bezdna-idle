# 🔍 Аудит сцены боя — «БЕЗДНА» (Abyss)

> **Дата:** 19 сентября 2025
> **Проект:** VK Mini Apps idle RPG — рогалик-поход за лутом
> **Стек:** React 18 + TypeScript + Vite + TailwindCSS / Node.js HTTP / PostgreSQL
> **Боевая система:** Не Godot-сцена, а **веб-симуляция на редьюсерах** (React + TS)

---

## ⚠️ ВАЖНО: это НЕ Godot-проект

Ищя `.tscn`-сцены и Godot-скрипты, я их не нашёл — потому что проект построен как **веб-приложение** (SPA), а не как Godot-геймплей. Бой реализован как **React-компонент + движок на редьюсерах** (Redux-паттерн). Всё боевое моделирование происходит в `src/game/logic.ts`, а рендеринг — в `src/ui/BattleScreen.tsx`.

---

## 1. Структура сцены боя

### Файлы, задействованные в бою:

| Файл | Назначение | Размер |
|------|-----------|--------|
| `src/game/logic.ts` | **Ядро боевой симуляции**: редьюсер, tick, урон, спавн врагов, убийства | 2865 строк |
| `src/ui/BattleScreen.tsx` | **UI сцены боя**: зона, враг, скиллы, HUD, эффекты | 250 строк |
| `src/ui/art.tsx` | **SVG-арт**: герой (2 класса), 19 мобов + 5 боссов | 365 строк |
| `src/ui/HeroAnimations.css` | **CSS-анимации**: idle, attack, hurt, death, class-specific | 296 строк |
| `src/game/useGame.tsx` | **React-контекст**: игровой цикл TICK каждые 100ms | 200 строк |
| `src/game/types.ts` | **TypeScript-интерфейсы**: BattleS, Enemy, HeroS, Stats и др. | 592 строки |
| `src/game/data.ts` | **Данные**: классы, скиллы, пассивки, зоны, мобы, сеты | 1204 строки |
| `src/game/sprites.ts` | **Спрайты мобов**: атлас PNG → варианты по элементу зоны | 97 строк |
| `src/game/logic.test.ts` | **Тесты**: баланс, крафт, прогрессия | 478 строк |

### Общая связность:
```
useGame.tsx (TICK каждые 100ms)
    └── reducer() в logic.ts
         ├── farmTick() — основной бой
         ├── runTick() — рогалик
         ├── duelTick() — PvP дуэль
         └── partyTick() — пати-подземелье
    └── BattleScreen.tsx рендерит текущее состояние s.battle
         ├── art.tsx → HeroArt / MonsterArt (SVG)
         ├── HeroAnimations.css → CSS keyframes
         └── sprites.ts → моб-спрайты по элементу зоны
```

---

## 2. Боевая система (архитектура)

### 2.1 Два режима боя

**Режим 1: Фарм (farmTick)** — основной, используется на карте
- Герой автоматически атакует врага каждый тик
- Враг автоматически атакует героя
- Скиллы кастятся автоматически по таймеру КД
- Тик: **100ms** (из `useGame.tsx`)
- Волны: каждая зона имеет регулярные волны, каждая 10-я — босс

**Режим 2: Рогалик (runTick)** — «Экспедиция» / «Портал Бездны»
- Фарм на паузе, когда герой в режиме рогалика
- 20 волн максимум, редкие луты
- Реликвии дают временные бонусы

### 2.2 Два класса

| | Маг (Mage) | Лучница (Archer) |
|---|---|---|
| **База урона** | 9.5 | 6.2 |
| **База AS** | 0.85 | 1.4 |
| **База HP** | 95 | 115 |
| **База Крит** | 5% | 9% |
| **Стиль** | Редкие, мощные спеллы | Частые, многократные удары |
| **Скиллы** | Огненный шар, Ледяной раскол, Метеор | Тройной выстрел, Гнилая стрела, Ливень стрел |
| **ДОП (DoT)** | Нет | Да (яд от Гнилой стрелы) |

### 2.3 Система скиллов (автокаст)

Скиллы — **автоматические**, без ручного управления:

| Скилл | Класс | КД | Тип | Механика |
|--------|-------|-----|------|----------|
| Огненный шар | Маг | 6с | burst | 210+45% урона ×1 попадание |
| Ледяной раскол | Маг | 10с | burst | 300+60% урона, замедление |
| Метеор | Маг | 24с | burst | 650+110% урона |
| Тройной выстрел | Лучн. | 6с | multi | 3×105+22% урона |
| Гнилая стрела | Лучн. | 9с | dot | 110+20% урона + 45+9% DoT/5с |
| Ливень стрел | Лучн. | 22с | multi | 8×70+13% урона |

**Примечание**: В боевой сцене нет кнопки ручного каста — скиллы кастуются сами когда КД = 0. Это обсуждается в `AUDIT_PLAN.md` (задача: добавить ручной каст или переименовать).

### 2.4 Система урона

```
Урон героя = stats.dmg × mult × (0.9 + random(0.2)) × (1 + crit/100 × critDmg/100 - 1)
Урон врага = e.dmg × (0.9 + random(0.2)) × (1 - armor/(armor+110))
Скилл     = stats.dmg × def.mult(lvl) × def.hits(lvl) × 0.6 × (1 + extraSkillDamagePct)
```

- **Крит**: шанс `stats.crit%`, множитель `critDmg%`
- **Броня**: `mit = armor / (armor + 110)` — стандартная формула MHW/D&D
- **Уклонение**: `stats.dodge%` — шанс пропустить удар врага
- **Life Steal**: `% от урона → HP`
- **DoT**: урон в секунду, применяется каждый тик

### 2.5 Цикл TICK (фарм)

```
tick(dt)
  ├── farmTick(s, dt)
  │     ├── Обновление таймеров (buffs, cds, statuses)
  │     ├── Автовоскрешение (если paused && respawnT > 0)
  │     ├── Обновление DoT (dotT -= dt, enemy.hp -= dotDps × dt)
  │     ├── Герой атакует (heroT += as × dt, while ≥ 1: heroHit)
  │     ├── Враг атакует (enemyT += as × dt, while ≥ 1: enemyHit)
  │     ├── Авто-каст скиллов (проверка CД, применение)
  │     └── Проверка смерти (killEnemy / enemyDie)
  │     └── Статусы: таймер уменьшается, удаляются когда t ≤ 0
  ├── runTick(s, dt) — если run.active
  ├── duelTick(s, dt) — если duel.state === "fight"
  └── partyTick(s, dt) — если party exists
```

---

## 3. Система врагов и зон

### 3.1 Зоны (5 штук)

| Зона | Тинтовый цвет | Мобы | Босс | Описание |
|------|--------------|-------|------|----------|
| Прелый лес | `#7dc95e` | Slime, Shroom, Wolf | Treant | Старт |
| Костяные пещеры | `#c9d4de` | Skel, Rat, Bat | BoneTyrant | |
| Логово разбойников | `#e0a34e` | Bandit, Thrower, Ogre | Ataman | |
| Цитадель Пустоты | `#9b7bd8` | Wisp, Golem, Acolyte | Devourer | |
| Бездна | `#ff6b8d` | Voidling, Golem, Wisp | Voidmaw | Бесконечная |

### 3.2 Масштабирование врагов

```
hp = (base + zone × perZone) × pow(hpPow, zone*12 + wave - 1) × (boss ? bossHpMult : 1)
dmg = (base + zone × perDmg) × pow(dmgPow, p) × (boss ? bossDmgMult : 1)
```

- Боссы каждые 10 волн (волна % 10 === 0)
- Если босс «отбит» — на его волне стоят усиленные стражи (×2.2 HP)
- Бездна (zone 4): дополнительный масштабирование после волны 10
- Каждое убийство: `wave += 1`, новый спавн

### 3.13 Типы мобов (16)

| Ключ | Название | HP/DMG роль |
|------|----------|-------------|
| slime | Слизень-бухгалтер | Базовый |
| shroom | Гнилошляп | |
| wolf | Лютый волк | |
| skel | Скелет-работяга | |
| rat | Крысолюд | |
| bat | Летуха-крикуха | |
| bandit | Разбойник | |
| thrower | Метатель ножей | Дальний |
| ogre | Огр-вышибала | Танк |
| wisp | Блуждающий огонёк | Лёгкий |
| golem | Голем-консьерж | Танк |
| acolyte | Послушник Пустоты | Маг |
| voidling | Отголосок Бездны | Бездна |
| treant | Гнилодрев | Босс леса |
| boneTyrant | Костяной Тиран | Босс пещер |
| ataman | Атаман Шлык | Босс разбойников |
| devourer | Пожиратель Пустоты | Босс цитадели |
| voidmaw | Отродье Бездны | Босс бездны |

### 3.4 Система дропа

- **Золото**: `round((4 + zone×6 + wave×1.4) × (boss ? 13 : 1) × (0.9+random(0.2)))`
- **XP**: `round((7 + zone×7 + wave×1.6) × depthReward × (boss ? 9 : 1))`
- **Шанс дропа предмета**: `dropBaseChance + luck/100`
- **Боссы**: +2 предмета, +1 ресурс крафта, +1 зелье
- **Пулы дропа**: настраиваются в админке (`getDropPools`)

---

## 4. Визуальная часть

### 4.1 HeroArt (арт героя)

- **SVG 100×130** viewBox
- **Два класса**: Mage (чёрная ряса + посох) и Archer (коричневая куртка + лук)
- **Экипировка**: 8 слотов → визуальные элементы на герое
- **Качество**: rarity 1-5 → цветовая индикация (зелёный → красный)
- **Анимации**:
  - `hero-breathe` — покачивание 2с
  - `hero-idle-head` — покачивание головы
  - `mage-robe-idle` — дрожание мантии мага
  - `archer-bow-idle` — покачивание лука
  - `hero-glow-legendary` — свечение для легендарных
  - `hero-damage-flash` — вспышка при уроне
  - `anim-shake` — тряска при получении урона
  - `anim-lunge-right/left` — шаг при атаке

### 4.2 MonsterArt (арт моба)

- **SVG 100×100** viewBox — 19 мобов с уникальными силуэтами
- **Спрайты**: PNG-атлас (`public/sprites/mobs/mob_XXX.png`), выбор по элементу зоны
- **Боссы**: отдельный крупный спрайт + `anim-boss` (покачивание)
- **Каждый моб** имеет уникальную цветовую палитру (c1/c2)

### 4.3 Система эффектов (fx)

```typescript
interface Fx {
  id: number;
  text: string;       // напр. "42", "+128", "-15", "CRIT!"
  kind: "dmg" | "crit" | "hurt" | "heal" | "gold" | "xp" | "combo";
  x: number;          // 0..100
  y: number;          // 0..100
  life: number;       // 0.95, уменьшается каждый кадр
}
```

- Максимум **16 эффектов** одновременно (боевой), 14 (дуэль)
- Плавающий текст с CSS-классами: `text-fog`, `text-gold2`, `text-blood` и т.д.
- `combo` — счётчик подряд-критов (x3, x4, x5...)

### 4.4 CSS-анимации

| Анимация | Длительность | Назначение |
|----------|-------------|-----------|
| `hero-idle` | 2с | Покачивание стоя |
| `hero-hurt` | 0.3с | Тряска при уроне |
| `hero-attack-mage` | 0.4с | Атака мага |
| `hero-attack-archer` | 0.35с | Атака лучника |
| `hero-death` | 0.8с | Увядание |
| `anim-shake` | — | Экран трясёт |
| `anim-lunge-left/right` | — | Шаг в сторону |
| `anim-float` | — | Парение моба |
| `anim-boss` | — | Покачивание босса |
| `anim-pop` | — | Появление баннера смерти |

---

## 5. Состояние боя (BattleS)

```typescript
interface BattleS {
  zone: number;           // Текущая зона (0-4)
  wave: number;           // Текущая волна (1+)
  enemy: Enemy | null;    // Текущий враг
  heroT: number;          // Таймер атаки героя (накопление AS)
  enemyT: number;         // Таймер атаки врага
  dotDps: number;         // Урон за секунду от DoT
  dotT: number;           // Оставшееся время DoT
  skillT: number;         // Таймер авто-каста
  cds: Record<string, number>; // КД скиллов
  fx: Fx[];               // Эффекты (урон, золото, XP)
  statuses: CombatStatus[];    // Активные баффы/дебаффы
  log: string[];          // Боевой лог (последние 6)
  paused: boolean;        // Пауза (смерть)
  respawnT: number;       // Таймер автовоскрешения
  bossLocked: boolean;    // Босс отбит (нужен ручной призыв)
  combo: number;          // Счётчик комбо-критов
}
```

### 5.1 Враг (Enemy)

```typescript
interface Enemy {
  key: string;            // Ключ моба (slime, wolf, treant...)
  name: string;           // Отображаемое имя
  hp: number;             // Текущее HP
  maxHp: number;          // Макс HP
  dmg: number;            // Урон
  as: number;             // Скорость атаки
  boss: boolean;          // Босс?
  gold: number;           // Награда золото
  xp: number;             // Награда XP
}
```

---

## 6. Аудит: проблемы и находки

### 🔴 Критические

| # | Проблема | Описание |
|---|----------|----------|
| 1 | **Нет ручного каста скиллов** | Скиллы — чистый автокаст. Игрок не контролирует использование скиллов. `AUDIT_PLAN.md` упоминает это как незавершённую задачу: "Добавить ручной каст-кнопки в Battle/Run/Duel только если нужен игровой контроль. Иначе переименовать раздел в «Боевые способности»" |
| 2 | **Смерть = пауза + автотор** | При смерти герой не может ничего делать — просто ждёт автовоскрешения. `B.paused = true` блокирует все действия. Нет возможности主动 использовать предметы или уйти |
| 3 | **Тик 100ms — серверная симуляция** | `useGame.tsx` вызывает `TICK` каждые 100ms. Это означает, что при лагах/медленном FPS бой может ускоряться или замедляться |
| 4 | **Одиночный `reducer` — нет разделения сервер/клиент** | Весь игровой цикл в одном reducer. Сервер использует `state-simulator.ts` для офлайн-дохода — но клиентский и серверный расчёт могут расходиться |

### 🟡 Средние

| # | Проблема | Описание |
|---|----------|----------|
| 5 | **Слишком много логики в одном файле** | `logic.ts` — 2865 строк. Все боевые системы (фарм, рогалик, дуэль, пати, гильдия) в одном файле. Сложно поддерживать и тестировать |
| 6 | **Маг слаб в фарме** | AS = 0.85 — маг атакует реже. Сила мага проявляется в скиллах (burst), но в фарме без ручного каста он проигрывает лучнику |
| 7 | **Система статусов (CombatStatus) не используется полноценно** | Интерфейс определён, но в `farmTick` нет обработки `statuses` для баффов. Баффы работают через `s.buffs`, а не через `BattleS.statuses` |
| 8 | **Боевой лог ограничен 6 записями** | `pushLog` усекает до 6 сообщений. Невозможно просмотреть историю боёв |
| 9 | **Нет визуальной индикации DoT врага** | Индикатор яда есть только у героя, но враг с DoT не визуально отличается |
| 10 | **Combo-система неполная** | `combo` инкрементируется при критах, но фактически используется только для визуального эффекта `x{n}!`. Нет механических последствий |

### 🟢 Мелкие / Замечания

| # | Замечание | Описание |
|---|-----------|----------|
| 11 | **16 эффектов одновременно — мало** | В `farmTick` лимит `fx` = 16, в `duelTick` = 14. При интенсивном бою эффекты быстро исчезают |
| 12 | **Перемешивание `dt`** | `Math.min((now - last) / 1000, 0.5)` — максимум 0.5с тик. При табуляции может пропустить много ходов |
| 13 | **Моб-арт SVG vs PNG** | Большинство мобов рисуются SVG (`art.tsx`), но есть PNG-спрайты (`sprites.ts`). Смешение двух систем — спрайты используются только для определённых зон |
| 14 | **`heroRef` и `artRef`** | В `BattleScreen.tsx` рефы используются только для CSS-классов анимации. Нет JavaScript-физики или интерактивности |
| 15 | **Нет партикулярных систем** | Все эффекты — это CSS-тексты и SVG-фильтры. Нет реальных частиц (искр, дыма, снежинок) |
| 16 | **`B.fx` обновляется в `farmTick` без рендер-помех** | `.slice(0, 16)` удаляет старые эффекты, но нет плавной анимации исчезновения (только `life` уменьшается) |
| 17 | **Зона Бездна (zone 4) — бесконечный фарм** | `endless: true` — нет лимита волн. HP и DMG масштабируются линейно с каждой волной после 10-й. Это делает поздний фарм бесконечно долгим |
| 18 | **Маг vs Лучница баланс** | Лучница имеет AS 1.4 vs маг 0.85, но маг делает урон 9.5 vs 6.2. В автокаст-режиме лучница стабильнее за счёт частых атак |

---

## 7. Потенциальные улучшения

### 7.1 Архитектурные

1. **Разделить `logic.ts`** на модули: `combat.ts`, `run.ts`, `duel.ts`, `party.ts`
2. **Добавить server-side authoritative tick** — клиент отправляет действия, сервер подтверждает состояние
3. **Интерполяция анимаций** — вместо жёсткого `dt` на клиенте, использовать серверный тик
4. **Состояние боя в Redux Toolkit** или аналог — для предсказуемых мутаций

### 7.2 Геймплейные

1. **Ручной каст скиллов** — добавить кнопку "Каст" рядом с каждым скиллом
2. **Защита от смерти** — добавить возможность использовать зелье/умереть "честно" с выбором
3. **Визуальные эффекты для DoT** — зелёные частицы вокруг врага с ядом
4. **Система комбо** — каждый крит даёт буст урона, стакаются до определённого лимита
5. **Побеги из боя** — возможность уйти с потерой прогресса/золота
6. **Тренировочный режим** — тренироваться без потери прогресса
7. **Скорость боя** — настройка скорости тика (x1, x2, x3) для быстрого фарма

### 7.3 Визуальные

1. **Canvas 2D / WebGL** для частиц вместо CSS
2. **Sprite sheets** для анимации атак (walk, attack, idle, death — отдельные кадры)
3. **Screen shake** на мощные удары (уже есть `anim-shake`, но не для мощных критов)
4. **Vignette/окраска** при низком HP (красная рамка)
5. **Атмосферные эффекты** — дым, искры, свечение при босс-появлении

---

## 8. Сравнение с аналогами

| Аспект | БЕЗДНА | Классический idle RPG |
|--------|--------|----------------------|
| Управление | Автокаст | Автокаст + ручной скиллы |
| Визуализация | SVG + CSS | Canvas / Sprite Sheets |
| Физика боя | Тиковый (100ms) | Тиковый / real-time |
| Структура кода | Monorepo reducer | ECS / OOP |
| Мобов | 16 | 50-200+ |
| Классов | 2 | 5-15 |
| Скиллов | 6 | 30-60 |

---

## 9. Итоговый вердикт

| Категория | Оценка | Комментарий |
|-----------|--------|-------------|
| **Архитектура кода** | ⭐⭐⭐ 3/5 | Monolithic reducer, 2865 строк — сложно ориентироваться |
| **Боевая механика** | ⭐⭐⭐⭐ 4/5 | Сбалансированный DPS, классы различаются, DoT/крит/броня работают |
| **Визуал** | ⭐⭐⭐⭐ 4/5 | Красивые SVG-арты, CSS-анимации, но нет частиц |
| **UX/UI** | ⭐⭐⭐⭐ 4/5 | Чистый дизайн, информативный HUD, но неполные текстовые эффекты |
| **Масштабируемость** | ⭐⭐⭐ 3/5 | Новые мобы/зоны легко добавлять через `data.ts`, но кодовая база тесная |
| **Баланс** | ⭐⭐⭐⭐ 4/5 | Лучница сильнее в фарме, маг сильнее в боссах — хороший дизайн классов |
| **Общая боевая сцена** | ⭐⭐⭐⭐ 4/5 | Работает стабильно, тесты покрывают основное |

**Статус: ✅ Боевая сцена РАБОТАЕТ, но нуждается в расширении визуальных эффектов и добавлении ручного управления скиллами**

---

## 10. Рекомендации по приоритетам

### P0 (нужно сделать):
1. Добавить ручной каст-триггер для скиллов (или переименовать раздел)
2. Разделить `logic.ts` на модули по игровым системам
3. Добавить защиту от смерти (зелье/побег/респавн с выбором)

### P1 (очень желательно):
4. Визуальные эффекты DoT на врагах
5. Увеличить лимит FX до 32
6. Добавить screen shake на мощные удары
7. Скорость боя (x1/x2/x3)

### P2 (будущие итерации):
8. Canvas/частицы для ударов
9. ECS-архитектура для боевой системы
10. Серверная авторизация боевых действий
