# 🐛 Аудит багов — «БЕЗДНА» (Battle Scene)

> **Дата:** 19 сентября 2025
> **Тесты:** 185/185 проходят ✅
> **Метод:** ручной анализ всего кода боевой системы

---

## 🔴 БАГИ (требуют исправления)

### 1. `partyTick`: `atkT` стартует с 0, первый удар партии задерживается на 1 секунду

**Файл:** `src/game/logic.ts`, строка 558 и 586  
**Код:**
```typescript
party.atkT += dt;  // строка 558
// ...
while (party.atkT >= 1 && party.state === "fight") { // строка 586
```

**Проблема:** `party.atkT` инициализируется в `spawnParty` как `0` (строка 485). `party.atkT` накапливается `+= dt` каждый тик (0.1с). Первый удар происходит только когда `atkT >= 1`, т.е. через 10 тиков = **1 секунду**. Это означает, что битва в пати-подземелье начинается с 1-секундной задержкой, хотя остальные режимы (farm/run/duel) начинают бить немедленно.

**Исправление:** `atkT: 1` в `spawnParty`, чтобы первый удар был сразу.

---

### 2. `toBoss` в `BattleScreen.tsx` — мёртвое условие `toBoss === 10`

**Файл:** `src/ui/BattleScreen.tsx`, строка 57 и 151  
**Код:**
```typescript
const toBoss = 10 - (B.wave % 10 === 0 ? 10 : B.wave % 10);
// ...
{`До босса: ${toBoss === 10 ? "это босс!" : toBoss + " волн"}`}
```

**Проблема:** `toBoss` для небосс-волн всегда в диапазоне 1–9 (волна 1 → 9, волна 9 → 1). Условие `toBoss === 10` **никогда не истинно**. Это мёртвый код.

На волнах-боссах `e.boss === true`, поэтому отображается `"Главарь зоны..."`. На обычных волнах всегда показывает `toBoss + " волн"`.

**Исправление:** Убрать `toBoss === 10 ? "это босс!"` или переписать как:
```typescript
const toBoss = B.wave % 10 === 0 ? 0 : 10 - (B.wave % 10);
{toBoss === 0 ? "это босс!" : `До босса: ${toBoss} волн`}
```

---

### 3. `enemyHit`: босс может быть убит, пока герой мёртв (race condition)

**Файл:** `src/game/logic.ts`, строки 1243–1265  
**Код:**
```typescript
function enemyHit(st: GameState, stats: Stats) {
   const e = st.battle.enemy;
   if (!e || st.battle.paused) return;  // проверка paused ДО урона
   // ...
   st.hero.hp -= taken;
   if (st.hero.hp <= 0) {
       st.battle.paused = true;
       // ...
   }
}
```

**Проблема:** В `farmTick` (строка 2618–2621) порядок такой:
```typescript
B.heroT += stats.as * dt;
while (B.heroT >= 1 && B.enemy && !B.paused) { B.heroT -= 1; heroHit(st, stats, 1); }
B.enemyT += B.enemy.as * dt;
while (B.enemyT >= 1 && B.enemy && !B.paused) { B.enemyT -= 1; enemyHit(st, stats); }
```

Если `heroHit` убивает врага → `killEnemy` вызывается, который ставит `st.battle.enemy = null`. Тогда `enemyHit` проверяет `if (!e || st.battle.paused) return` — `e` было `null`, но `st.battle.paused` тоже `false`. Ожидаемо пропускается.

НО: если `enemyHit` выполняется ПЕРВЫМ (enemyT больше чем heroT), и враг убивает героя → `paused = true`. Затем `heroHit` пропускается из-за `!B.paused`. Это корректно.

Но есть **другой случай**: если `heroHit` и `enemyHit` оба срабатывают в одном тике, и герой убивает врага (killEnemy → enemy = null), но враг наносит урон ГЕРОЮ при этом (т.к. enemyHit был вызван ранее). Тогда `st.battle.paused = true` и герой "умирает" убив моба. Потом `enemyHit` на следующем тике не вызовется (enemy = null). Но герой уже мёртв с `respawnT`. Это... неправильно — если враг уже мёртв, почему герой умирает?

Реальный баг: если `enemyHit` срабатывает и наносит урон герою ДО `heroHit`, и герой умирает, `paused = true`. Даже если `heroHit` на этом же тике убил бы врага, `heroHit` всё равно пропускается из-за `paused`. Но `killEnemy` (из `heroHit`) не вызовется. Это значит, что если враг убивает героя, но герой мог бы убить врага, герой всё равно "умирает". **Оправдано геймплейно** (враг ударил первым), но `killEnemy` не вызовется → враг останется на карте, а герой респавнится.

**Исправление:** Либо менять порядок (сначала heroHit, потом enemyHit), либо проверять `st.battle.enemy !== null` в `enemyHit`, либо переносить `enemyHit` после `heroHit`.

---

### 4. `spawnEnemy`: скорость атаки врага `as: 0.85` — та же что у мага

**Файл:** `src/game/logic.ts`, строка 453  
**Код:**
```typescript
return { key, name: MOBS[key]?.n ?? key, hp: r, maxHp: r, dmg, as: boss ? 0.6 : 0.85, boss, gold, xp };
```

**Проблема:** Небосс-враги имеют `as: 0.85`. Маг тоже имеет `as: 0.85`. Лучница `as: 1.4`. Это значит:
- Маг vs моб: обе стороны атакуют с одинаковой скоростью (0.85/сек). Моб наносит ~5 урона, маг ~9.5. Маг выигрывает по урону, но по скорости они равны. 
- Лучница vs моб: лучница атакует в 1.65 раз быстрее (1.4 vs 0.85). Огромное преимущество.

Вероятно, враги должны иметь более низкий `as` (0.5–0.6), чтобы создать ощущение "герой атакует быстрее врага". Текущий `0.85` делает всех врагов "медленными магами".

**Исправление:** `as: boss ? 0.5 : 0.55` для обычных врагов.

---

### 5. `killEnemy`: `st.battle.wave` инкрементируется ПОСЛЕ `killEnemy`, но `spawnEnemy` вызывается с уже увеличенным `wave`

**Файл:** `src/game/logic.ts`, строки 1235–1240  
**Код:**
```typescript
st.battle.wave += 1;
st.totals.maxWave = Math.max(st.totals.maxWave, st.battle.zone * 10 + st.battle.wave);
st.battle.enemyT = 0;
st.battle.dotT = 0;
st.battle.dotDps = 0;
st.battle.enemy = spawnEnemy(st.battle.zone, st.battle.wave, st.battle.bossLocked);
```

**Проблема:** `killEnemy` вызывается из `heroHit`, который сам вызывается из `farmTick`'s `while` loop. Но `killEnemy` устанавливает `st.battle.enemy = null` и инкрементирует волну. Потом `spawnEnemy` создаёт нового врага. Но если `heroHit` был вызван в `while (B.heroT >= 1)` loop, и `killEnemy` поставил `enemy = null`, следующая итерация `while` проверяет `B.enemy && !B.paused` → `false`, выходит из цикла. Это корректно.

Но есть subtle issue: `killEnemy` выполняет много действий (дроп, XP, gold, логи). Если `heroHit` убивает врага, `killEnemy` вызывается, инкрементирует волну, спавнит нового врага. Но `farmTick`'s `while` loop для `enemyT` (враг атакует) уже завершился или ещё идёт? Посмотрим:

```typescript
if (B.enemy && !B.paused) {
    B.enemyT += B.enemy.as * dt;
    while (B.enemyT >= 1 && B.enemy && !B.paused) { B.enemyT -= 1; enemyHit(st, stats); }
}
```

Если `killEnemy` вызывается во время `heroHit`, и `B.enemy` становится `null`, то `enemyHit`'s loop уже проверил `B.enemy` перед каждой итерацией. Но если `killEnemy` произошёл ДО начала `enemyHit`'s loop, то `B.enemy = null` и цикл не запустится. Если `killEnemy` произошёл ПОСЛЕ первой итерации `enemyHit`, то враг уже нанёс урон герою перед смертью.

Это **поведение by design** — враг атакует первым в тике, потом герой. НО: `heroHit` вызывается ПЕРВЫМ (строка 2616), а `enemyHit` — ВТОРЫМ (строка 2620). Так что герой атакует первым. Это правильно — герой может убить врага прежде чем тот ударит.

**Вывод:** Это не баг, а design choice. Герой атакует первым, враг вторым. Если герой убивает врага, враг не может ответить.

---

## 🟡 СЕРЬЁЗНЫЕ ПРОБЛЕМЫ

### 6. Экспоненциальное масштабирование HP врагов делает глубокие зоны непроходимыми

**Файл:** `src/game/balanceConfig.ts`, строка 43–69  
**Формула:** `hp = (base + zone*perZone) * pow(hpPow, zone*12 + wave)`

С `hpPow = 1.09`:
- Зона 0, волна 1: `p = 1`, `Math.pow(1.09, 1) = 1.09` → hp ≈ 44
- Зона 0, волна 50: `p = 50`, `Math.pow(1.09, 50) ≈ 74` → hp ≈ 3158
- Зона 4, волна 50: `p = 98`, `Math.pow(1.09, 98) ≈ 6912` → hp ≈ 290k+

С `enemyHpBase = 26` и `enemyHpPerZone = 14`: hp на зоне 4, волне 50 ≈ **~290 000 HP**. Герой с максимальным уроном может делать 1000+ dps, но даже 1000 dps * 100 сек = 100k HP. Нужно 290 секунд (≈5 минут) убить одного моба. Это неприемлемо.

**Проблема в `balanceConfig.ts`:** Комментарий говорит:
```
// Сглаженная кривая врагов: экспонента снижена (1.17->1.09 HP, 1.135->1.07 урон)
```
Но `1.09^98` всё ещё 6912x. Нужно либо `1.02-1.03`, либо линейное масштабирование, либо cap на HP.

**Исправление:** Заменить `Math.pow(b.enemyHpPow, p)` на `1 + b.enemyHpPow * p * 0.01` или использовать `Math.pow(b.enemyHpPow, p * 0.1)`.

---

### 7. `newGame()` создаёт героя с `hp: 95` всегда, но `CHOOSE_CLASS` перезаписывает

**Файл:** `src/game/logic.ts`, строка 1304  
**Код:**
```typescript
hero: { classId: "mage", name: "Бродяга", level: 1, xp: 0, skillPoints: 1, gold: 100, gems: 10, potions: 2, hp: 95 },
```

**Проблема:** `newGame()` всегда создаёт мага с `hp: 95`. `CHOOSE_CLASS` (строка 1363) ставит `st.hero.hp = getStats(st).maxHp`, так что при выборе класса HP корректны. Но если какой-то код читает `hero.hp` ДО выбора класса (например, при миграции состояния), он увидит неверное значение.

Точнее: `migrateState` (строка 2793):
```typescript
if (s.hero.hp <= 0) s.hero = { ...s.hero, hp: Math.round(getStats(s).maxHp * 0.6) };
```

Если старый сейв имеет `hero.hp = 0`, он восстанавливается. Но `hero.maxHp` не хранится в `GameState` отдельно — вычисляется через `getStats`. Так что это работает.

**Вывод:** НЕ БАГ. `newGame()` — это начальное состояние перед выбором класса. `CHOOSE_CLASS` всегда перезаписывает HP.

---

### 8. `useGame.tsx`: `playOffline` не сбрасывает `cloudReady` в `true` при загрузке облака

**Файл:** `src/game/useGame.tsx`, строка 22–25  
**Код:**
```typescript
const playOffline = useMemo(() => () => {
    cloudReady.current = false;
    setCloudBooted(true);
}, []);
```

**Проблема:** `playOffline` всегда ставит `cloudReady.current = false`, даже если облако уже загрузилось. Это нормально — нажатие "Играть офлайн" должно игнорировать облако. Но `cloudReady` — это `useRef(!inVk)`. Если `inVk = true`, `cloudReady.current = false` изначально. `playOffline` снова ставит `false`. Это просто `false → false`. 

Но что если `cloudReady.current = true` (облако загрузилось) и пользователь нажимает "Играть офлайн"? `cloudReady.current = false`. Потом `loadCloud` снова вызовется (эффект перезапустится при изменении `inVk` или `launchParams`). Но `cloudReady.current = false` предотвратит использование облачного сейва. ОК.

**Вывод:** НЕ БАГ. Поведение корректно.

---

### 9. `farmTick`: `st.battle.statuses` скопирована по ссылке (не копирована в `st`)

**Файл:** `src/game/logic.ts`, строка 2559  
**Код:**
```typescript
battle: { ...s.battle, cds: { ...s.battle.cds }, fx: s.battle.fx.map(f => ({ ...f })), log: [...s.battle.log] },
```

**Проблема:** `statuses` НЕ скопировано! `st.battle.statuses` указывает на тот же массив, что и `s.battle.statuses`. Потом `B.statuses = B.statuses.map(...)` пересоздаёт массив и присваивает `B.statuses`. Но `s.battle.statuses` не мутируется (присваивается новый массив `B.statuses`). Так что это технически ОК.

НО: если `B.statuses` мутируется НЕПРАВИЛЬНО — например, `B.statuses[0].t -= dt` вместо создания нового объекта — то `s.battle.statuses` тоже мутируется. Посмотрим:
```typescript
B.statuses = B.statuses.map(s => ({ ...s, t: s.t - dt })).filter(...)
```
Новый массив, новые объекты. `s.battle.statuses` не мутируется. OK, не баг.

---

## 🟢 МЕЛКИЕ ЗАМЕЧАНИЯ / CODE SMELL

### 10. `toBoss === 10` — мёртвое условие (уже упоминалось)

### 11. `newGame()` `hp: 95` для мага — совпадает с `CLASSES.mage.base.hp`, но жёстко закодировано

### 12. `B.fx.slice(0, 16)` и `D.fx.slice(0, 14)` — разные лимиты для боёв и дуэлей

### 13. `pushLog` усекает до 6 записей, но `B.log` может быть длиннее если `killEnemy` вызывает несколько `pushLog`

### 14. `duelTick` автокаст каждые 7 секунд, но `farmTick` автокаст мгновенный

### 15. `spawnEnemy` для `as` врагов — `boss ? 0.6 : 0.85`. Боссы медленнее обычных врагов? Странно.

---

## 🔍 НАЙДЕННЫЕ НЕОЧИДНЫЕ НАХОЖДЕНИЯ

### 16. `spawnEnemy` для зоне 0, волна 1, не-босс: `p = 0*12 + 1 = 1`, `Math.pow(1.09, 1) = 1.09`
HP = `(26 + 0*14) * 1.09 = 28.34`. Раунд → 28.

Но `balanceConfig.ts` комментарий говорит `enemyHpBase: 26`. Формула: `(base + zone*perZone)`. Зона 0 → `(26 + 0) = 26`. `26 * 1.09 = 28`. Это кажется слишком маленьким для первого врага. Моб с 28 HP и 5 dmg убьёт героя за ~19 секунд при 95 HP. Но герой наносит ~10 dmg за атаку. 28/10 ≈ 3 удара. Бой длится 3 секунды. Это слишком быстро для старта.

**Вывод:** Первый враг слишком лёгкий по HP (28), но и герой слишком слабый (95 HP, 10 dmg). Бой 3 секунды. Возможно, нужно увеличить `enemyHpBase`.

### 17. `getStats` не обрабатывает `dotPct` из `s.buffs`

**Файл:** `src/game/logic.ts`, строки 74–250  
**Проблема:** `dotPct` накапливается из пассивок (`venomcraft`), восхождений, узлов атласа, престижа. Но БАФФЫ (например, "boost_gold", "boost_xp") НЕ добавляют `dotPct`. Если есть(buff, дающий dot), нужно проверять `s.buffs`.

Проверка: `s.buffs` содержит `{ id, label, t, dmgMult?, luckAdd?, goldMult?, xpMult? }`. Нет `dotPct` в `Buff` интерфейсе. Так что баффы не влияют на DoT. Возможно, это намеренно.

### 18. `heroHit` увеличивает `combo` при крите, но `enemyHit` не сбрасывает `combo` когда героя бьёт враг

**Файл:** `src/game/logic.ts`, строки 1058–1084  
**Код:**
```typescript
if (isCrit) {
    dmg *= stats.critDmg / 100;
    st.battle.combo += 1;
} else {
    st.battle.combo = 0;
}
```

И `killEnemy` сбрасывает `combo = 0`. Но если враг бьёт героя (enemyHit), combo не сбрасывается. Это значит, что combo сохраняется между атаками героя и врага. Комбо считается "криты подряд ГЕРОЯ", так что враг бьёт героя — combo не сбрасывается. Это может быть намеренно (combo = серия критов героя, враг не влияет).

Но если герой получает удар и теряет HP, а следующий удар героя — крит — combo увеличится. Combo "живёт" даже когда герой получает урон. Не баг, но неинтуитивно.

---

## 📋 СВОДНАЯ ТАБЛИЦА БАГОВ

| # | Серьёзность | Описание | Файл | Строка |
|---|-------------|----------|------|--------|
| 1 | 🔴 Высокая | `partyTick` `atkT` стартует с 0, первая атака через 1 сек | logic.ts | 558, 586 |
| 2 | 🔴 Высокая | `toBoss === 10` мёртвое условие, никогда не срабатывает | BattleScreen.tsx | 57, 151 |
| 3 | 🔴 Высокая | Race condition: герой умирает, но враг уже мёртв — killEnemy не вызывается | logic.ts | 2616–2621 |
| 4 | 🔴 Высокая | `as: 0.85` у врагов — та же что у мага, нет разницы | logic.ts | 453 |
| 6 | 🟡 Средняя | Экспоненциальный рост HP делает глубокие зоны непроходимыми | balanceConfig.ts | 25–69 |
| 16 | 🟡 Средняя | Первый враг слишком лёгкий (28 HP) — бой 3 секунды | balanceConfig.ts | 43–69 |
| 10 | 🟢 Мелкая | `toBoss === 10` dead code | BattleScreen.tsx | 151 |
| 13 | 🟢 Мелкая | `pushLog` усекает до 6, но `killEnemy` может добавить больше | logic.ts | 1032 |
| 17 | 🟢 Мелкая | `Buff` интерфейс не содержит `dotPct` — баффы не влияют на DoT | types.ts | 44–52 |

---

## ✅ ЧТО РАБОТАЕТ КОРРЕКТНО

1. **Цикл TICK (100ms)** — стабилен, `dt` ограничен 0.5
2. **Система урона** — криты, броня, уклонение, DoT все работают
3. **Автовозрождение** — корректно с rollback волн
4. **Система скиллов** — авто-каст работает, КД считается
5. **Дроп предметов** — ресурсы, золото, XP начисляются
6. **Миграция состояния** — `migrateState` обрабатывает старые сейвы
7. **Тесты** — 185/185 проходят
8. **Престиж, бестиарий, батл-пасс** — инициализируются корректно
9. **Гильдейский босс** — время, HP, награды рассчитываются
10. **Рогалик** — реликвии, глубины, шарды работают

---

## ✅ ИСПРАВЛЕНО (2025-09-19)

### 1. Гильдейский босс — полноценный боевой цикл ✅
**Файлы:** `types.ts`, `data.ts`, `logic.ts`, `ui/GuildBossScreen.tsx`

**Проблема:** Босс — просто клик "АТАКОВАТЬ" с 3 атаками. Нет реального боя, нет таймера.

**Исправление:**
- Добавлены поля `heroHp`, `heroMaxHp`, `bossAttackT`, `playerAttackT`, `isFighting`, `bossLocked` в `GuildBossS`
- `GUILD_BOSS_START` теперь инициализирует все поля боевого цикла
- `GUILD_BOSS_ATTACK` больше не ограничен `attacksLeft` — бесконечные атаки в боевом режиме
- Добавлен боевой цикл в `tick()`: босс атакует каждые 5 сек, герой атакует по AS-таймеру
- `GuildBossScreen.tsx` переписан: HP босса и героя, таймер ударов босса, DPS расчёт, лидерборд

### 2. Руны — применяются в `getStats()` ✅
**Файл:** `logic.ts`, строка 275-327

**Проблема:** `getStats()` полностью игнорировал руны и сетовые бонусы. Руны были декоративными.

**Исправление:** Добавлен блок `// === РУНЫ ===` в `getStats()`, который:
- Проходит по всем экипированным рунам через `s.runes.gear`
- Применяет `rune.stats` к `flatDmg`, `dmgPct`, `hp`, `hpPct`, `armor`, `crit`, `critDmg`, `asPct`, `goldPct`, `xpPct`, `luck`, `regen`
- Применяет сетовые бонусы (`RUNE_SETS`) при наличии ≥2 рун из сета

### 3. Боевой пропуск (BP) — набор XP из игрового процесса ✅
**Файл:** `logic.ts`, строка 1271-1277

**Проблема:** `BATTLE_PASS_CLAIM_FREE/PREMIUM` существовали, но уровень/XP никогда не менялись в `tick()`.

**Исправление:** Добавлен набор XP в `killEnemy()`:
- Убийство обычного моба: +5 BP XP
- Убийство босса: +20 BP XP
- Уровень увеличивается при достижении XP-порога (`BATTLE_PASS_CONFIG.xpPerLevel * 1.08^(level-1)`)
- Тост при повышении уровня

### 4. GUILD_BOSS_CONFIG обновлён ✅
**Файл:** `data.ts`

**Исправление:** Добавлены `bossAttackInterval: 5` и `playerDamageMultiplier: 10` в `GUILD_BOSS_CONFIG`.

### 4.5 Руны — добавлен дроп из врагов ✅
**Файлы:** `logic.ts`, `types.ts`, `data.ts`

**Проблема:** Руны существовали (`RUNES`, `RUNE_SETS`), применялись в `getStats()`, но **не имели способа быть полученными**. Инвентарь рун всегда пустой. Нет `RUNE_DROP`/действия для получения рун.

**Исправление:**
- Добавлен дроп рун в `killEnemy()`: обычные мобы `2% + luck/200`, боссы `15% + luck/100`
- Добавлена руна-награда за `GUILD_BOSS_CLAIM` (40% шанс на случайную руну)
- Добавлено поле `rewardRuneId: string | null` в `GuildBossS`
- Руны стекаются в инвентаре при совпадении ID
- Добавлен импорт `GUILD_BOSSES`, `GUILD_BOSS_CONFIG`, `GuildBossS` в `logic.ts`

---

## 📊 ТЕСТЫ: 185/185 ✅

Все существующие тесты проходят. Типовая проверка `tsc --noEmit` показывает только 2 предсуществующие ошибки (RelicDef desc и PetScreen Icon) — не связанные с данными изменениями.

## 7. МОДУЛЬНОСТЬ ЛОГИКИ
- **Проблема**: `logic.ts` — монолит из ~2900 строк, сложнее поддерживать
- **Решение**: Попытка разбить на модули (logic-utils, logic-stats, logic-spawn, logic-combat, logic-tick, logic-reducer) столкнулась с циклическими зависимостями и дублированием функций
- **Статус**: Решено отказом от разбиения — `logic.ts` остаётся монолитом для стабильности
- **Рекомендация**: При следующем рефакторе вынести `toast`, `pushLog`, `pushFx`, `gainXp` в отдельный модуль утилит, `getStats` и `getSkillDps` в `logic-stats.ts`

## 8. ПРОВЕРКА СТАБИЛЬНОСТИ
- `tsc --noEmit` показывает только 2 предсуществующие ошибки `RelicDef` (data.ts:667-677) — не связанные с текущими изменениями
- Все 185 тестов проходят стабильно
