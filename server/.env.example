PORT=3001
DATABASE_URL=postgres://game_api:change-me@127.0.0.1:5432/abyss_idle
VK_APP_ID=54071751
# Обязательно: секрет приложения из настроек VK Mini Apps (Каталог → Приложение → Настройки).
# Без него verifyLaunchParams всегда возвращает null и вся авторизация падает (бейдж «офлайн»).
VK_APP_SECRET=
VK_SERVICE_TOKEN=
CORS_ORIGIN=https://vk.ru
